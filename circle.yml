# circle.yml
general:
  branches:
    only:
      - master # list of branches to build
      - /feature-.*/ # or regexes
machine: # 虛擬機的環境
  node:
    version: 9.5.0 #把剛剛得到的node version填到這裡
  test:
    - npm test  #每一個deploy到github上的commit都會執行npm test